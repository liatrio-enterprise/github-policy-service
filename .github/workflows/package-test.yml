on:
  registry_package:
  release:

name: testing package event - yay

jobs:
  package: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v5
        with:
          script: |
            console.log(context.event)